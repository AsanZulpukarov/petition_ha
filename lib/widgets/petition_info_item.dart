import 'package:flutter/material.dart';

class PetitionInfoItem extends StatelessWidget {
  const PetitionInfoItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
