import 'package:flutter/material.dart';

class KodeksItem extends StatelessWidget {
  const KodeksItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
